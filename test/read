 : Une ligne vide dans la map :  --- > check
 : Un caractère incorrect dans la map, genre un 4 -- > check
 : Une map ouverte -- > check
 : La map est avant un autre élément -- > pas encore check
 : Il n’y a pas de map ----> pas encore check
 : Pas de joueur ou plusieurs joueurs --- > [as encore check]


 FAIS T PUTAIN DE LEAKS ET PROTEGE T MALLOC
 merci bien

 int		recursivemap(t_cub *cub, int x, int y)
{
	printf("%d, %d\n", x,y);
	if (x <= 0 || y <= 0 || x > cub->x || y > cub->y - 1|| cub->map[x][y] == 0)
	{
		printf("1");
		cub->open = 1;
	}
	if (cub->map[x][y] != '0')
	{
		if (cub->map[x][y] == ' ')
			cub->open = 1;
		printf("2");
	}
	if (cub->open == 0)
	{
		printf("3");
		cub->map[x][y] = '9';
		recursivemap(cub, x + 1, y);
		recursivemap(cub, x, y - 1);
		recursivemap(cub, x - 1, y);
		recursivemap(cub, x, y + 1);
		recursivemap(cub, x + 1, y + 1);
		recursivemap(cub, x - 1, y - 1);
		recursivemap(cub, x + 1, y - 1);
		recursivemap(cub, x - 1, y + 1);
	}
	if (cub->open == 1)
		return (-1);
	return (1);
}